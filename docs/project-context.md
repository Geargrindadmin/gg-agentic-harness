---
generated_at: "2026-03-09"
generator: "scripts/generate-project-context.mjs"
source_files:
  - "package.json"
  - "mcp-servers/gg-skills/package.json"
  - "packages/gg-cli/package.json"
  - "packages/gg-control-plane-server/package.json"
  - "packages/gg-core/package.json"
  - "packages/gg-orchestrator/package.json"
  - "packages/gg-runtime-adapters/package.json"
---

# Project Context for AI Agents

_Selective BMAD adoption: project-context generation + adversarial review discipline._

---

## Technology Stack & Versions

- **Node.js Engine**: `unspecified` (from `package.json`)
- **TypeScript**: `^5.9.2`
- **React**: `not declared`
- **MongoDB Driver**: `not declared`
- **Playwright Test**: `not declared`
- **Playwright CLI**: `not declared`
- **Jest / ts-jest**: `not declared` / `not declared`
- **ESLint / Prettier**: `not declared` / `not declared`
- **Payments/Comms SDKs**: Stripe `not declared`, SendGrid `not declared`, Twilio `not declared`

## Critical Implementation Rules

### Governance
- `docs/PRD.md` is the product source of truth; `Task.md` is execution checklist.
- For `TASK|TASK_LITE|DECISION`, mirror run state with `node scripts/gws-task.mjs ...`.
- Architectural changes require ADRs in `docs/decisions/ADR-NNN.md`.

### Build and Validation Gates
- Minimum completion gate: `npx tsc --noEmit`, `npm run lint`, relevant tests.
- Before completion claims, apply `verification-before-completion` discipline with command evidence.
- For UI flows, enforce screenshot-backed checks via Playwright tooling.

### Security and Reliability
- Follow `.agent/rules/anti-mocking.md`; allowlist-only mocks.
- Apply adversarial review protocol from `.agent/rules/adversarial-review.md` for review tasks.
- Never commit secrets; use env indirection for sensitive tokens and credentials.

### Agentic Operating Rules
- Runtime selection and MCP set are profile-driven via `docs/runtime-profiles.md` and `.agent/registry/mcp-runtime.json`.
- `CLAUDE.md` is canonical prompt contract; `AGENTS.md` mirrors it exactly.
- For agent handoffs, enforce file-claim and serialization from `docs/agentic-harness.md`.

## BMAD Cherry Picks in GGV3

- **Project context workflow**: generated by `scripts/generate-project-context.mjs`, surfaced through `.agent/workflows/generate-project-context.md`.
- **Adversarial review**: explicit forced-findings review rule in `.agent/rules/adversarial-review.md`.

## Usage

- Regenerate after major architecture/tooling updates: `npm run harness:project-context`.
- CI drift check: `npm run harness:project-context:check`.
- Keep this file concise; link to source rule files for detail.

